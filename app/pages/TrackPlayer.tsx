import { FlatList, StyleSheet, Text } from 'react-native';

const TrackPlayer = () => {
    return (
        // <FlatList
        //     data={tracks}
        //     renderItem={({ item }) => <PlayerItem track={item} />}
        //     showsHorizontalScrollIndicator={false}
        // />
        <Text>Track List</Text>
    );
}

export default TrackPlayer